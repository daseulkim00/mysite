<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="guestbook">
	<!-- insert select update delete mapping 하면된다 -->
	<insert id="insert" parameterType="guestbookvo">
		<![CDATA[
 			insert into guestbook values (null, #{name }, #{password }, #{message }, now())
		]]>
		<selectKey keyProperty="no" resultType="long" order="AFTER">
		<!-- select된것의 마지막 키 값을가져오는 함수 -->
			<![CDATA[
				select last_insert_id();  
			]]>
		</selectKey>
	</insert>
	
	<insert id="insert02" parameterType="guestbookvo">
		<![CDATA[
 			insert into guestbook values (null, #{name }, #{password }, #{message }, now())
		]]>
	</insert>
	
	<delete id="delete" parameterType="guestbookvo">
		<![CDATA[
 			 delete from guestbook where no=#{no } and password=#{password }
		]]>
	</delete>
	
	<select id="findAll" resultType="guestbookvo">
		<![CDATA[
			 select no, name, date_format(reg_date, '%Y/%m/%d %H:%i:%s') as regDate, message 
			 	from guestbook 
			 order by reg_date desc
		]]>
	</select>
</mapper>
